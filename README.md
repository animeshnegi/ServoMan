# ServoMan
ERVOMAN is a **lightweight** self-hosted server &amp; website control panel (cPanel / aaPanel class) with features those panels don't ship: an AI ops assistant, security scoring, git push-to-deploy, record-verified email sending and anomaly detection. Single Node process, ~120 MB RAM.  -
